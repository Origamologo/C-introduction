COMPILE
$ g++ main.cpp -o hello

COMMENTS
// patata
/*
patatas fritas
*/

SINTAX
# include <iostream> //Common library we need in order to get access to cout functio
Every statement mus end with semicolon ;

VARIABLES
They are named in camel case and it should describe what the variable is doing
Provide a way to store date whithin a program. Describe program statement   

    · Unique name
    · Type that determines the kind of data to be stored
    · Value can be retrieved, reassigned or manipulated

std::endl end line command is a better way to jump lines using variables than \n

type name = value;
    type name //declaration of the variable
    = value; //assignment of the variable

We must create the variable before we try to assign a value to it
    //type name;
    //name =value;

    · BOOLEANS
    The simplest of variables that take the least amount of memory, is True or Flase
    bool isOn = true;
    It prints in binary 1 or 0

    · INTEGERS
    Positive, negative or 0 (default value is 0)
    Signed or unsigned (unsigned always positive)
    Can be replace with shorts, longs or long longs (whole numbers representing different rages of values)

    int age = 42;
    uint age = 42; // Only hold a possitive value, if we assing a negative value to uint we'll get a hughe number due to binary transformation behind the scenes

    short //small range of numbers
    long //long range
    long long //very large numbers

    · DOUBLES
    Can be decimal or whole number
    Can be positive, nagative or 0
    Can replace with floats or long doubles 
        Floats: have fewer decimal points
        Long double: very long decimal

    double balance = -5.95;

    The default number for a double is not 0 but an extremely tiny decimal very close to 0: 4.94066e-324

    · CHARACTERS
    Represent any single letter, number or symbol, but:
        Must be exactly 1 in length
        Must be between single quotes ''
        Each character has an unique unicode representation

    char comma = ',';

    For numbers, although the representation in the console will look the same if it's an int or a char, they are completly different values, one being a whole number and the other an unicode value
    
    · STRINGS
    Text data:
        List of characters strung together (is king of treated as a list of characters)
        Can be any length including 0
        Must be between double quotes ""
        Have a suite of functions to retrieve properties or perform manipulations

    As is part of the standard library, be must call it to declare strings
    std::string name = "Zeneva";

    A numerical representation of the string is not the same as the number

    · CONTANTS
    Constants are variables whose value cannot change.
        Look and act like regular variablesAre identified by the "const" keyword

    const std::string email = "mymail@mail.com"

    A lot of the functionalities in C++ requires constants in stead of variables.
    Also, contants retrict our code to do what it should if we want a variable whitha  value that should never change

    · USER INPUT
    Intake and process user text input:
        Fetch user input via cin
        Store the input
        Modify input

    std::string input;
    std::cin >> input;

    As soon as there is a space is assumed that is a new input, so from "Hello, World!" it will print "Hello,", this can lead to leftovers in the Input Buffer that will be catch in any future input corrupting the program.

OPERATORS

    · ARITHMETIC
    Mini-function that aloow us to either modify values or produce new results by combinig values
        Arithmetic: + - * / %

    int age = 28;
    int newAge = age + 1;

    We can combine different types of numbers
    We can chage the original value if we want

    int age1 = 28;
    age1 = age1 + 1;

    We can use + to strings in order to append

    · ASSIGMENT
    Assings some value to a variable
    Can be combined with arithmetic operators




